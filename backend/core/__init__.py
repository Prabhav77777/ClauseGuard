"""
MODULE: Core application utilities package initialization.

@level-one-validation: Core package marker containing shared rate limiters and prompt helper formatters. Pure import namespace, no standalone runtime execution.

#Scope-Of-Improvement: Could centralize custom middleware definitions here if middleware count grows beyond current security headers.
"""
